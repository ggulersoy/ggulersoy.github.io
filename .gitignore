# Hugo build output (regenerated by CI on every deploy)
/public/
/resources/
.hugo_build.lock

# OS / editor cruft
.DS_Store
*.swp

# Node (pagefind, if run locally)
/node_modules/

# Local Claude Code settings (machine-specific permissions)
.claude/settings.local.json
